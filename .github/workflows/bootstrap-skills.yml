name: Bootstrap SEO Skills

on:
  push:
    branches: [main]
    paths:
      - '_staging/READY'

permissions:
  contents: write

jobs:
  bootstrap:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Rebuild and unpack repository payload
        run: |
          cat _staging/payload.part.* > /tmp/seo-skills.b64
          base64 -d /tmp/seo-skills.b64 > /tmp/seo-skills.zip
          rm -rf /tmp/seo-skills-bootstrap
          mkdir -p /tmp/seo-skills-bootstrap
          unzip -q /tmp/seo-skills.zip -d /tmp/seo-skills-bootstrap
          cp -a /tmp/seo-skills-bootstrap/SEO-Skills/. .
          rm -rf _staging
          rm -f .github/workflows/bootstrap-skills.yml
          rmdir .github/workflows 2>/dev/null || true
          rmdir .github 2>/dev/null || true
      - name: Commit final repository contents
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m 'feat: add keyword research skills'
          git push
