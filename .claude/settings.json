{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^Bash$",
        "hooks": [
          {
            "type": "command",
            "command": "ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0; [ -f \"$ROOT/runtime/stage_hook.py\" ] || exit 0; command -v python3 >/dev/null 2>&1 || exit 0; cd \"$ROOT\" || exit 0; exec python3 runtime/stage_hook.py pre",
            "timeout": 10,
            "statusMessage": "Checking SEO stage contract"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0; [ -f \"$ROOT/runtime/stage_hook.py\" ] || exit 0; command -v python3 >/dev/null 2>&1 || exit 0; cd \"$ROOT\" || exit 0; exec python3 runtime/stage_hook.py stop",
            "timeout": 10,
            "statusMessage": "Checking SEO run completeness"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0; [ -f \"$ROOT/runtime/stage_hook.py\" ] || exit 0; command -v python3 >/dev/null 2>&1 || exit 0; cd \"$ROOT\" || exit 0; exec python3 runtime/stage_hook.py stop",
            "timeout": 10,
            "statusMessage": "Checking SEO run completeness"
          }
        ]
      }
    ]
  }
}
